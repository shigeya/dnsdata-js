# dnsdata-js

DNS/DNSSEC protocol implementation in TypeScript.

Sibling implementation: [dnsdata-go](https://github.com/shigeya/dnsdata-go).

This is a Lerna monorepo. The core library lives in [`packages/core/`](packages/core/) and is published as `@dnsdata/core`.
